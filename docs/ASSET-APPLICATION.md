# 已确认资产应用到主游戏

2026-09-19。主工程为 `deskfront-jevlab`，主游戏入口 **http://127.0.0.1:8768/**；`8773` 是独立资产预览，不代表主游戏的当前状态。

## 核对与应用结果

| 资产 | 主游戏状态 |
|---|---|
| 三阵营纯色玩具兵 | 保留主游戏已升级的骨骼、根运动、战斗姿态和布娃娃；当前 29 动作，不用预览的 22 动作覆盖 |
| T2 坦克、枪械、火箭筒、手雷 | 已由实际单位和武器事件加载；保留主游戏后续模型整理与挂点适配 |
| 反坦克炮 | 已接入炮手接管、推行、部署、攻击装甲和弃炮 |
| 男性办公人物 | 本轮补齐遗漏：旧 Casual 坐姿角色替换为已确认的 Quaternius Business Man；62 骨骼、8 秒打字循环，运行播放速率 0.6 |
| 大桌面、极简办公室、沙包、弹坑、小楼 | 已在三个可选地图中运行；保留原有灯光、主游戏地图位置及可破坏规则 |
| 武器声音、爆炸与透明烟雾 | 已接入真实开火、弹丸命中、爆炸事件；保留主游戏后续声音变体和渲染调整 |
| 楼梯寻路修复 | 平台门槛已存在；本轮补接带角色胶囊与地面支撑检查的通路图、贴墙退离和受阻重规划，保持主游戏根运动与撤离排队 |

主游戏规则继续负责伤害、弹药、控制权、地图胜负与 Agent 状态。没有用资产样片的时间轴替换游戏 AI。当前炮组仍为主游戏的单炮手机制；这次不新增多炮手职责，也不声称 Jev 专属 SDK 已连接。

## 文件与许可

- 运行人物：`assets/models/office-worker.glb`。当前源文件、原始 Business Man、生成脚本与 CC0 许可在 `source/latest/office-worker/` 和 `source/latest/tools/build_office_worker.py`。
- 旧 Casual 的 Blender、源 GLB、当时运行 GLB和生成器保留在 `source/archive/office-worker-casual/`，归档生成器只写归档目录。
- `scripts/building_navigation.gd` 为从已确认预览适配的建筑通路；`scripts/tactical_building.gd` 连接主游戏驻防、根运动与失败恢复。各层三个火力位延续主游戏布局。
- `tools/audit_asset_application.py` 对照 22 项主要运行资产、预览哈希、资源许可和主游戏注册哈希；不同的主游戏修订保留，不直接退回预览文件。
- `docs/asset-integration/asset-provenance.json` 与 `.forge/assets.json` 记录更新后的男性人物来源和哈希。其余模型、音频和字体保留各自许可。

## 验收

- 新人物：原始 GLB 审计和 Godot 实际导入审计通过。
- 新增主游戏应用检查：17/17，覆盖实际 Business Man 实例、士兵动作保留、三个一楼火力位、不同上楼时机撤离后再进驻，以及撞墙后恢复。
- 原有资源集成检查：89/89；设施检查 16/16，三人进驻与撤离通过；离线 Python 接口测试 23/23。
- 浏览器真实点击后音频上下文进入 running，并记录实际音效事件。
- Forge build 和严格资源许可 release 门禁通过，已真实导出 Web。
- 浏览器资源交互 13/13、设施交互 6/6，使用实际策划台按钮执行换图、装备切换、坦克增援、上楼、撤楼、接管炮与弃炮；完整结果、截图与构建哈希见 `docs/evidence/asset-application/`。

实测使用单独的本地 8776 服务，避免抢占 8768 上用户正在玩的控制会话；两者读取同一主工程和 Web 导出。最终交付入口仍为 8768，刷新后加载新版。文档中的通过项仅对应证据记录的构建；不把其他并行工作的后续修改自动算作已验收。

并行的 LM 更新产生了新的运行构建号；资源交互、设施交互与最终声音检查分别记录自己的构建号，三者对应同一资源束哈希，详见 `verification.json`。
