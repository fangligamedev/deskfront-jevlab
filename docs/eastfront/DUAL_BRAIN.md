# 快慢脑东线：规划、施工与连续战斗

## 如何体验

进入「无尽东线」，默认前沿构筑为 **DeepSeek 规划 + Laya 本地执行**，远征队为 **Laya · 本地战术指挥**。Laya 指挥小队，Godot 连续落实掩体分工与交替掩护；绿色控制权保持 `lm`。也可选择 DeepSeek + JEV，并将远征队设为 JEV。双脑中的模型指挥按小队意图执行，独立单模型模式仍支持逐兵请求。详见 [战术修复报告](TACTICAL_DEBUG.md)。

默认组合需要 Ark 配置和本地 Laya 服务；JEV 组合需要 Ark 与 TypeSafe 的 Key。单独选 JEV / DeepSeek 仍保留原来的单模型布局模式；本地生成不需要网络。玩家接管和外部 Agent 控制期间，JEV 的队伍意图不会覆盖玩家或 Agent 指令。

## 分工

| 层级 | 工作 | 频率与边界 |
|---|---|---|
| DeepSeek 慢脑 | 结合存活兵种、当前战场和最近防线，规划未来三段不同布局、2–3 名守军武器与工位、施工次序、双方坦克出场延迟与支援/前压角色 | 剩余两段方案时预取下一批，保留尚未使用的方案；每局最多 64 次 |
| JEV / Laya 快脑 | 在持续推进、南/北侧翼、伤员重整中选择意图，并执行施工优先级、守军据壕/交叉火力/后撤、双方坦克放行 | 目标间隔 2 秒，最多一个在途请求；每局最多 300 次 |
| Godot 执行器 | 路径、跑动、掩护、射击、保命、挖掘搬运的连续动画，以及伤亡与占点 | 本地逐帧执行；不等待模型逐步指挥脚步 |

两类模型请求在独立任务中运行。慢脑独立等待上限 20 秒，不沿用逐兵决策的短超时。JEV 网络等待上限 5 秒；模型性能并不等于本地每帧更新速度。超时、欠费、旧局次、过期战术都不会冻结战场。401/402/403 或欠费停止该脑本局重试；预算耗尽后已建战場继续由本地执行器运行，新关卡等待慢脑恢复；不会偷偷切成本地随机模板。来源、错误和调用计数在页面和 LLM 日志中可见。

慢脑规划只可使用已登记模板，不接受可执行代码或任意模型文件。当前六种布局为错列沙包、双翼交叉火力、反装甲突破口、曲折浅战壕、补给站包围战、窄口封锁线；有不同的配色、掩体走向、旗点偏移和敌方武器。相邻布局不得重复。当前场景仍是平坦桌面，没有真实山地高差或任意地形雕刻。

## 可见施工

每段由两名复用 `assets/models/toy-soldier.glb` 的塑料骨骼工兵完成三个工位，依次展示：

1. 勘测工位。
2. 挥动铲子挖掘，沟底和土堆逐渐出现。
3. 往返后方料堆，抱着沙包返回。
4. 弯腰堆砌，逐袋显现沙包。

均衡流水线一般约 12 游戏秒完成；JEV 也可先让工兵挖通所有工位，再集中搬运堆砌；模型调度只改变尚未开始的工位优先侧，不会倒退已经完成的工作。全部工事完成后，才原子提交碰撞、射线遮挡与导航，再让该段守军参战。施工区域处于未开放前沿，不会在活动士兵脚下突然长出障碍。

工兵是施工表现实体，不是新增可射击战斗兵种。浅战壕目前由沟底外观、土堆和真实沙包掩体表达，没有下沉地形通行系统。人体复用当前塑料士兵网格、骨架与跑动/蹲姿动画，铲子和沙包用双臂 IK 持握；没有方块人体。道具与浅壕为代码几何，资源来源与许可沿用现有素材记录。

## 推进与存活

- 无威胁时，健康队员同时沿已验证安全通路连续推进，最长约 0.95 米；不会每走 0.24 米就等另一轮网络请求。
- 临近敌方武器有效范围时，转入原有掩护、低暴露转移与射击；JEV 的侧翼意图影响合法移动点的选择。
- 游戏 AI 或双脑模型小队控制下，血量低于 40% 的伤员退回后方十字补给点。只有到达补给点且附近没有敌方火力威胁才按每秒 9 HP 治疗，恢复到 85% 后归队。治疗不提供无敌。
- 占点补给保留。绿色步兵全部阵亡后（即使坦克仍存活）等待 5 游戏秒，从左侧后方部署一支新的三人小队，编号递增；不复活旧编号，不重置已夺取地段或模型预算。
- 演示默认关闭鼠标贴边平移，防止页面切换时镜头漂移；仍可用 B 开启，键盘、拖动和缩放继续可用。

## 接口与日志

`eastfront_start.backend` 新增 `dual_brain`。快脑仅通过 console 权限提交：

```json
{"action":"eastfront_directive","sector":3,"intent":"flank_north","construction":"north_first","defense":"entrench","armor":"deploy_both","provider":"typesafe_jev_fast","instance_id":"当前会话","run_id":"当前局次"}
```

`intent`：`advance/flank_north/flank_south/regroup`；`construction`：`balanced/north_first/south_first/dig_first/sandbag_first`。非当前扇区或非法枚举拒绝。意图有效期至少 5 游戏秒，随倍速扩展，之后本地执行器继续推进。`defense` 为 `entrench/crossfire/fallback`；`armor` 为 `hold/deploy_green/deploy_red/deploy_both`。

`state.eastfront` 新增 `wave/reinforcement_in/recovering/directive/directive_source`；每段 `construction` 提供进度、工兵动作和位置。`lm.frontier` 提供慢脑规划、快脑结果、在途请求、预算与错误。

日志单位筛选：`eastfront-slow-brain`、`eastfront-fast-brain`。慢脑的 `planned` 表示方案已校验，不等于全部三段已经落地；具体落地以 `chunk_committed` 事件和实际场景为准。快脑指令有真实引擎回执。

## 验证

见 [交付报告](DELIVERY.md)。100 段压力检查仍通过人工推进占点来覆盖流式回收，不当作 100 场 AI 战斗胜利。

## 本地快脑替代

现在可选 **DeepSeek 规划 + Laya 本地执行**，替换快脑 JEV，保留原施工和战术执行机制。也支持 Laya 独立选防线与逐兵指挥，详见 [Laya 接入说明](LAYA.md)。

## 可执行慢脑方案（2026-09-21）

每个 `eastfront_propose` 可以携带 `battle_plan`，与当前关卡请求的 `sequence` 绑定：

```json
{"template":"staggered","defenders":[{"weapon":"rifle","station":0},{"weapon":"rocket","station":2}],"defense":"entrench","construction":"dig_first","armor":{"green":{"enabled":true,"delay":2,"role":"support"},"red":{"enabled":true,"delay":4,"role":"push"}},"reason":"据壕交错防守，装甲支援突破"}
```

- `station` 为模板的 0/1/2 号沙包工位，2–3 名守军不可重复工位；寻路、遮挡、身体四角保护与占位互斥由引擎验证。工事失效后重新寻找可用掩体。
- 横向工位在端部增加保护槽并加宽，以容纳完整士兵身体；浅壕位于工事背面。当前仍是桌面浅壕表现，不是可挖穿地形。
- 第一批首段必须安排双方坦克；我方有装甲时，守方必须有火箭兵。坦克从可见的己方后方位置待命；快脑只能在慢脑计划时间到后放行，经可达路线驶入，入场完成前不能开火。
- 每方最多复用一辆存活坦克；后段 `enabled` 表示安排/复用装甲，不会把存活坦克强行删除。新段更新其战术角色；玩家接管期间不自动放行绿色坦克。
- 日志保留慢脑原始输入、完整方案、队列提交和引擎回执；方案被拒后重新规划。`planned` 不代表已施工完成；`chunk_committed` 才表示碰撞与导航已经落地。
- `state.eastfront.chunks[].battle_plan` 是实际关卡方案，`armor[]` 提供待命/驶入/参战阶段、可放行时间、来源和角色。施工快照包含模型名、骨骼数、动作和位置。

## 战斗与右侧施工并行

当前段进入战斗后，立即请求并施工右侧下一段，不再等待绿色队员走近地图边缘。右侧工兵施工仅展示在前沿区域，未完成的工事不会突然改动当前战场碰撞、士兵路线、旗点或镜头；完工后守军先作为可见预备队待命。当前旗点拿下即可启用已经准备好的下一段，全程不重载或暂停对局。

慢脑剩余两段已规划内容时，后台预取下一批三段；新返回与旧缓存合并，每段分别关联原始模型调用和执行回执。世界仍只提前施工一段，保留有限流式窗口；网络故障时继续现有战斗并显示后台等待，不以本地随机布局冒充模型结果。长期断网超过已有缓冲时仍可能耗尽新关卡，不能承诺无限网络故障下无等待。
