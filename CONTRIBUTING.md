# 贡献指南

欢迎通过 Issue 和 Pull Request 改进项目。中文文档与沟通，稳定 API 字段使用英文。

## 开发流程

1. 按 [README](README.md) 和 [开发指南](docs/DEVELOPMENT.md) 运行项目。
2. 新功能说明要解决的问题和验收方式，复杂机制先在 Issue 讨论。
3. 从 main 创建工作分支，保持修改聚焦，不混入本机配置与无关资源。
4. 更新相关说明与测试，再提交 PR。

## 实现约定

- Godot 是唯一战斗状态来源，网页与模型不能伪造伤害或回执。
- 新动作同步更新 data/action-catalog.json、[动作契约](docs/AGENT_ACTION_STATES.md) 及校验。
- 保持四种控制模式的权限边界，处理重开、死亡和异步回复过期。
- 资源保留来源、许可和可编辑原稿；旧生成器不能覆盖当前资产。
- 不提交真实 Key、私人日志、生成缓存。测试使用明确占位凭证。
- 遵循所修改文件的格式，不进行整库无关格式化。

## 验证

```sh
python3 tools/check_project.py
python3 -m unittest discover -s tests -p 'test_*.py' -v
git diff --check
```

玩法、模型或 GDScript 修改需要 Godot 导入和完整项目测试；UI 修改需要真实 Web 输入和截图；战术修改需要有对应固定种子的行为断言。具体命令见开发指南。

真实 LM 测试由贡献者主动运行并承担费用；不要把凭证提供给维护者，也不要把本地兜底统计成模型决策。

## PR 与许可

说明问题、最终行为、测试和剩余限制。失败时保留原因，不能删除断言掩盖缺陷。提交代码/文档表示你有权按 MIT 提供；素材使用其单独标明的许可。未提供来源或无权再分发的文件不能合入。

发布遵循 [发布流程](docs/RELEASING.md)。既有标签不可移动；v9.19 是发布标记，运行时仍为 0.6.0。
