# 办公桌战场改造交付

2026-09-19 · 构建 `main-95e33487c32f` · 新默认地图「办公桌 · 文件争夺」。

## 已完成

- 战斗桌面保持左边界 X=-0.415 m，向右由 1.91 m 扩为 **3.82 m**，右边界 X=3.405 m；同步重建桌板、木质包边、支撑脚、抽屉柜和房间地面。左侧电脑与打字人物保持位置。
- 新地图直接使用桌面表面 Y=0.838 m；移除可见沙盘底座、围边、道路色块、小楼与沙包。没有把原沙盘简单拉伸。
- **7 种办公用品，16 处战术物件**：书堆、文件夹、键盘、纸盒、打印机、订书机、杯子。新增模型全部由本地 Blender 生成，提供可编辑 .blend 与 GLB，来源 CC0，许可与哈希已登记。
- 书堆、文件夹和键盘提供低掩体；打印机和纸盒阻挡视线、子弹与移动；高物件周边提供探身站位。纸盒及部分用品可被击毁，外观、碰撞、掩体预约和寻路网格同步更新。打印机与杯子保留为坚固障碍。
- 旗点位于中央便签处，东西两翼及北侧都有接近通路。三队各 3 名士兵，红方坦克从后方桌面驶入。初始导航路长绿队约 1.95–2.07 m、蓝队约 1.64–1.77 m、红队约 1.89–2.01 m；这是通路可达性与初始距离检查，尚不等于大规模对战胜率平衡证明。
- 保持原有办公室画风、光照和士兵材质；保留上一版的根运动、冲锋加速、防滑及贴地修复。

## 实机与测试

- 新地图 **80 项 Godot 检查全部通过**：九人出生点、夺旗可达性、多个旗点入口、模型/掩体身份对应、弹道遮挡、低掩体保护、纸盒破坏与坦克通路。
- 实际浏览器中完成画布左键选兵、右键移动、绕行办公用品、到旗点开始 30 秒计时、坦克驶入及切换游戏 AI。音频解锁通过，记录帧率 120 FPS（本机 Apple M4 Max 硬件加速；非所有设备保证）。网页测试使用隔离的确定性测试 LM，没有调用云模型。
- 全量 `python3 tools/project.py test` 通过，包含 **57 项 Python 测试**、语义/战斗/夺旗/模型接口/动画/贴地/布娃娃，以及四张地图的集成检查。
- 8 个新 GLB 的容器及 Godot 导入审计通过；严格素材许可审查、Godot Forge Web 发布门禁通过。
- 浏览器只有独立记录的 favicon 404 警告，没有游戏脚本、shader 或页面运行错误。

## 入口及配置

打开 http://127.0.0.1:8768/ 并刷新，新局默认进入「办公桌 · 文件争夺」。现有页面也可从「战场」下拉选择该地图（index=3）。旧三张地图仍作为兼容与回归地图保留。AI 创作沙盒的旧模板仍对应旧三张地图；本次新办公室地图由下列 JSON 配置驱动，尚未扩展该创作器的办公用品生成 schema。

- 地图布局、物件尺寸、材质标识、出生点：`data/sandbox-maps.json` 中 `office-front`。
- Blender 源程序：`source/latest/tools/build_office_battlefield.py`。
- 可编辑资产：`source/latest/office-battlefield-generated/*-editable.blend`。
- 运行资源：`assets/models/office-expanded.glb` 与 `office-*.glb`。
- 地图装配：`scripts/sandbox_map.gd`；碰撞/掩体数据转换：`scripts/map_rules.gd`。

## 证据

`contracts.json`、`web.json`、`web-top-game.png`、`web-battle-game.png`、`office.png`、`top.png`；详细日志在 `output/office-*.log`，发布清单在 `.forge/releases/release-web.json`。
