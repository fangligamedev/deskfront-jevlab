# 最新资产与动作合入主游戏报告

## 交付入口

主工程：`deskfront-jevlab`。主游戏：http://127.0.0.1:8768/ 。版本：`0.6.4-integrated.1`。

此前 8775 的预览与 8768 的主工程分别维护，预览里的更新没有自动进入主游戏。这次已将预览实现合入主工程，并重新导出 `build/web`；浏览器必须重新加载一次，才能替换已经运行在内存中的旧游戏。

## 已集成

- 纯色三阵营士兵：50 根骨骼、29 个动作，使用最新修正过的人体动作资源。
- 冲锋加速与减速、根运动位移、步频匹配；蹲姿横移和接地处理使用最新控制器。
- 真实蒙皮脚底采样和支撑相接地修正；跑步自然腾空相保留。贴地功能兼容主工程的楼层高度。
- 有关节限位的爆炸布娃娃和受击方向冲量。
- T2 坦克、步枪、手枪、火箭筒、手雷及武器挂点配置。
- 烟雾半透明着色器、特效池、武器音效变体与音频优先级。
- 办公室人物采用主工程并行任务刚升级的商务人物，没有用更旧的预览人物覆盖它。其他办公室极简场景与整体光照方向保持。
- 保留主工程的 LLM / Agent 控制、楼梯与驻守、反坦克炮及炮组功能。

从预览迁入 21 个变更资产，其中人物最终保留主工程较新的版本；每项来源与 SHA256 见 `transferred-assets.json` 和资产许可清单。

## 防止再次加载旧版

- 游戏内及控制台显示运行版本，API 返回 `build_id` 和 `resource_hash`。
- `tools/run.py` 启动时对脚本、场景、资产、数据和导出配置生成指纹；与已导出版本不一致时重新导出。
- 根目录新增 `启动最新版桌面前线.command`，固定启动实际主工程。
- 8775 为旧效果评审入口，后续实际游玩使用 8768。

## 验证

- Godot 语义检查 113 项、集成检查 89 项通过。
- 战斗碰撞 31 项通过，修复模型枪口尚未跟随瞬移单位时使用旧坐标的问题。
- LM、炮组与楼梯契约检查通过；Python 接口测试 23 项通过。
- 冲锋、29 动作贴地检查通过；2349 个人体姿态采样无失败。
- 9 个布娃娃均稳定落地，骨段最大伸长约 0.0000012。
- 真实 Web 浏览器验证：九名 50 骨骼士兵、人物打字、三地图切换、换枪、根运动长距离推进、手雷库存与爆炸、T2 增援、俯视镜头，全部通过；无控制台或着色器错误。
- 点击实际游戏画布后，Web AudioContext 为 running，采样率 48 kHz。验证音频解锁，不等于人工听感评审。
- Forge Web 发布门禁与真实导出通过。最终导出标识与浏览器证据见 `web-smoke.json`。

## 范围说明

本次交付实际 Web 游戏更新。未声称 macOS 安装包已重新签名、公证或验收。动作是否足够自然仍可在主游戏近景中继续视觉评审；上述骨骼与接地检查验证数值约束，不代替艺术质量评价。
