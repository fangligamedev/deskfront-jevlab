# 安全说明

当前维护 main 与最新发布标记 v9.19；旧标签保留用于复现，不承诺回补。

## 部署边界

项目是本机开发工具。服务只监听 127.0.0.1，并检查浏览器 POST 的 Origin；当前没有用户认证、TLS 或多实例鉴权。不要把控制端口通过代理或公开隧道暴露给不可信访问者。Origin 检查不能替代身份认证。

## 凭证

- Ark Key 保存在被忽略的 .env、环境变量或外部 dotenv，只提交 .env.example。
- LM 由服务端向允许的官方 HTTPS 地址请求，拒绝重定向。
- output/ 日志含状态和决策，分享前检查内容。
- 每局预算在重开时重置，不能替代供应商账户消费限制。
- Key 误提交时先在供应商侧撤销或轮换，再处理 Git 历史；只删除最新文件不能撤销泄漏。

## 报告

不要在公开 Issue 中发布 Key、私人日志或可直接利用的细节。先通过 [维护者 GitHub 主页](https://github.com/fangligamedev) 联系，确认私密报告渠道后提供复现和影响版本。本项目不承诺固定响应时限。

普通玩法问题使用 [错误报告模板](.github/ISSUE_TEMPLATE/bug_report.md)。
