# 快慢脑东线：规划、施工与连续战斗

## 如何体验

进入「无尽东线」，前沿构筑选 **快慢脑 · DeepSeek 规划 + JEV 执行**，远征队控制选 **游戏 AI · 小队协同**，点击「开始新东线」。这组配置中，JEV 下达战术意图，游戏 AI 执行，不需要另外把每名士兵切成逐兵 LLM。

本机服务需要同时配置 Ark 与 TypeSafe 的 Key。单独选 JEV / DeepSeek 仍保留原来的单模型布局模式；本地生成不需要网络。玩家接管和外部 Agent 控制期间，JEV 的队伍意图不会覆盖玩家或 Agent 指令。

## 分工

| 层级 | 工作 | 频率与边界 |
|---|---|---|
| DeepSeek 慢脑 | 结合存活兵种、当前战场和最近防线，规划未来三段不同布局及攻防理由 | 缓存耗尽后再请求；每局最多 64 次 |
| JEV 快脑 | 在持续推进、南/北侧翼、伤员重整中选择意图，并选择先挖后砌、优先搬运或施工优先侧 | 目标间隔 2 秒，最多一个在途请求；每局最多 300 次 |
| Godot 执行器 | 路径、跑动、掩护、射击、保命、挖掘搬运的连续动画，以及伤亡与占点 | 本地逐帧执行；不等待模型逐步指挥脚步 |

两类模型请求在独立任务中运行。JEV 网络等待上限 5 秒；模型性能并不等于本地每帧更新速度。超时、欠费、旧局次、过期战术都不会冻结战场。401/402/403 或欠费停止该脑本局重试；预算耗尽后继续由本地执行器推进。来源、错误和调用计数在页面和 LLM 日志中可见。

慢脑规划只可使用已登记模板，不接受可执行代码或任意模型文件。当前六种布局为错列沙包、双翼交叉火力、反装甲突破口、曲折浅战壕、补给站包围战、窄口封锁线；有不同的配色、掩体走向、旗点偏移和敌方武器。相邻布局不得重复。当前场景仍是平坦桌面，没有真实山地高差或任意地形雕刻。

## 可见施工

每段由两名原创代码建模的关节式玩具工兵完成三个工位，依次展示：

1. 勘测工位。
2. 挥动铲子挖掘，沟底和土堆逐渐出现。
3. 往返后方料堆，抱着沙包返回。
4. 弯腰堆砌，逐袋显现沙包。

均衡流水线一般约 12 游戏秒完成；JEV 也可先让工兵挖通所有工位，再集中搬运堆砌；模型调度只改变尚未开始的工位优先侧，不会倒退已经完成的工作。全部工事完成后，才原子提交碰撞、射线遮挡与导航，再让该段守军参战。施工区域处于未开放前沿，不会在活动士兵脚下突然长出障碍。

工兵是施工表现实体，不是新增可射击战斗兵种。浅战壕目前由沟底外观、土堆和真实沙包掩体表达，没有下沉地形通行系统。美术为本项目原创代码几何与现有已登记沙包资源，无新增外部下载资产。

## 推进与存活

- 无威胁时，健康队员同时沿已验证安全通路连续推进，最长约 0.95 米；不会每走 0.24 米就等另一轮网络请求。
- 临近敌方武器有效范围时，转入原有掩护、低暴露转移与射击；JEV 的侧翼意图影响合法移动点的选择。
- 游戏 AI 模式下，血量低于 40% 的伤员退回后方十字补给点。只有到达补给点且附近没有敌方火力威胁才按每秒 9 HP 治疗，恢复到 85% 后归队。治疗不提供无敌。
- 占点补给保留。绿色全员阵亡后等待 5 游戏秒，从左侧后方部署一支新的三人小队，编号递增；不复活旧编号，不重置已夺取地段或模型预算。
- 演示默认关闭鼠标贴边平移，防止页面切换时镜头漂移；仍可用 B 开启，键盘、拖动和缩放继续可用。

## 接口与日志

`eastfront_start.backend` 新增 `dual_brain`。快脑仅通过 console 权限提交：

```json
{"action":"eastfront_directive","sector":3,"intent":"flank_north","construction":"north_first","provider":"typesafe_jev_fast","instance_id":"当前会话","run_id":"当前局次"}
```

`intent`：`advance/flank_north/flank_south/regroup`；`construction`：`balanced/north_first/south_first/dig_first/sandbag_first`。非当前扇区或非法枚举拒绝。意图有效期 5 游戏秒，之后本地执行器继续推进。

`state.eastfront` 新增 `wave/reinforcement_in/recovering/directive/directive_source`；每段 `construction` 提供进度、工兵动作和位置。`lm.frontier` 提供慢脑规划、快脑结果、在途请求、预算与错误。

日志单位筛选：`eastfront-slow-brain`、`eastfront-fast-brain`。慢脑的 `planned` 表示方案已校验，不等于全部三段已经落地；具体落地以 `chunk_committed` 事件和实际场景为准。快脑指令有真实引擎回执。

## 验证

见 [交付报告](DELIVERY.md)。100 段压力检查仍通过人工推进占点来覆盖流式回收，不当作 100 场 AI 战斗胜利。

## 本地快脑替代

现在可选 **DeepSeek 规划 + Laya 本地执行**，替换快脑 JEV，保留原施工和战术执行机制。也支持 Laya 独立选防线与逐兵指挥，详见 [Laya 接入说明](LAYA.md)。
