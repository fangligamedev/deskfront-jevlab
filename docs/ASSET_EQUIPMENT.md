# 已准备资源的运行集成

- 反坦克炮：复用 `combat-review-v2` 准备的 `antitank-gun.blend` 和 GLB，CC0 原创，复制到 `source/latest`；运行模型 `assets/models/antitank-gun.glb`，比例 0.62，前向 -Z。原稿、生成脚本、Hash 和导入审计均保留。枪盾、轮架、炮管与握把保持原模型。
- 两层小楼：复用该工程的 `tactical-building.blend`、`building-components.json` 与原始运行组件脚本。适配器 `scripts/tactical_building.gd` 依照相同尺寸实例化 46 个构件，以便墙/窗/楼板分别承受射线和破坏。沿 z 平移 -1.57m，中心从 `[1,2.70]` 到 `[1,1.13]`；两层层高 .19m、外置 12 级楼梯。不是把整栋房子做成一个无法进入的碰撞盒。
- 三张地图均使用右上角蓝方基地配置。移开占用入口和楼梯的旧掩体，蓝方初始位置在楼外；游戏 AI 会自行进驻，LM/Agent/玩家可选择楼层和撤离。
- 火炮用简化轮架碰撞体和保守扫掠通道；炮管没有昂贵逐三角碰撞。楼梯使用连续斜坡碰撞，显示仍是阶梯。单兵沿实际高差运行，窗口弹道按真实高度检测。
- 源美术 CC0；运行代码 MIT。两层房屋是开放屋顶的战术观察模型，不是全屋室内建筑模拟。当前炮为单兵轻型玩具炮，不包含多炮手职责交接动画。
